// frontend/postcss.config.mjs (FINAL CORRECTED)

/** @type {import('postcss').Config} */
const config = {
  plugins: {
    // Ensure these plugins are correctly referenced
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;